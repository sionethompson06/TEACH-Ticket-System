# TEACH Ticket System — Decision Log

This log records confirmed architectural and service-design decisions, along with decisions that remain open. Confirmed entries use the date the decision was recorded. Status values are `Confirmed`, `Proposed`, or `Superseded`.

## Confirmed Decisions

| ID    | Date       | Decision                                                                                                                                                                                                                                                                                                                                                       | Status    | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Revisit Trigger                                                                                                                     |
| ----- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| D-001 | 2026-08-12 | IT and Facilities are the MVP departments.                                                                                                                                                                                                                                                                                                                     | Confirmed | These two departments generate the highest current volume of informal requests and have clear, well-understood workflows to model the core platform on.                                                                                                                                                                                                                                                                                                                                      | Leadership decides to sequence a different department first.                                                                        |
| D-002 | 2026-08-12 | Authentication uses Google Workspace, restricted to verified `@teachps.org` accounts.                                                                                                                                                                                                                                                                          | Confirmed | TEACH already provisions staff through Google Workspace; reusing it avoids a second identity system and keeps access tied to employment status.                                                                                                                                                                                                                                                                                                                                              | TEACH changes identity provider or domain strategy.                                                                                 |
| D-003 | 2026-08-12 | `TAT-56` and `TAT-78` are modeled as two separate service locations under one school (TEACH Academy of Technologies).                                                                                                                                                                                                                                          | Confirmed | The two campuses have distinct addresses and grade bands and need independent routing/reporting, even though they share one school identity.                                                                                                                                                                                                                                                                                                                                                 | The two campuses are consolidated into a single physical site.                                                                      |
| D-004 | 2026-08-12 | The Director of IT is the default triage owner for new IT tickets.                                                                                                                                                                                                                                                                                             | Confirmed | Centralizes initial triage accountability while the IT queue is small (three staff).                                                                                                                                                                                                                                                                                                                                                                                                         | IT staffing or org structure changes materially.                                                                                    |
| D-005 | 2026-08-12 | The Facilities Plant Manager is the initial Facilities queue owner and resolver.                                                                                                                                                                                                                                                                               | Confirmed | Facilities currently has one staff member; this reflects actual staffing during the MVP.                                                                                                                                                                                                                                                                                                                                                                                                     | Additional Facilities staff are hired.                                                                                              |
| D-006 | 2026-08-12 | Support hours are 7:30 a.m.–4:30 p.m. Pacific Time.                                                                                                                                                                                                                                                                                                            | Confirmed | Matches the standard TEACH staff workday for realistic service-target calculation.                                                                                                                                                                                                                                                                                                                                                                                                           | TEACH changes standard operating hours.                                                                                             |
| D-007 | 2026-08-12 | Weekends and configured school holidays are excluded from service clocks.                                                                                                                                                                                                                                                                                      | Confirmed | Service targets should reflect working time, not calendar time, so on-call expectations stay realistic.                                                                                                                                                                                                                                                                                                                                                                                      | TEACH adopts weekend or holiday on-call support.                                                                                    |
| D-008 | 2026-08-12 | Principals can view nonconfidential tickets for their assigned campus(es).                                                                                                                                                                                                                                                                                     | Confirmed | Gives campus leadership operational visibility without exposing confidential or cross-campus content.                                                                                                                                                                                                                                                                                                                                                                                        | Leadership changes campus-visibility policy.                                                                                        |
| D-009 | 2026-08-12 | Staff can submit requests on behalf of another employee or campus.                                                                                                                                                                                                                                                                                             | Confirmed | Front-office and support staff frequently submit requests for others; the system must track this accurately rather than misattributing tickets.                                                                                                                                                                                                                                                                                                                                              | Not anticipated; revisit only if abuse patterns emerge.                                                                             |
| D-010 | 2026-08-12 | The Director of IT and Chief Academic Officer are the initial system administrators.                                                                                                                                                                                                                                                                           | Confirmed | Matches current organizational accountability for technology and academic operations.                                                                                                                                                                                                                                                                                                                                                                                                        | System administrator list is formally revised.                                                                                      |
| D-011 | 2026-08-12 | Email notifications will be added immediately after the core workflow is validated, not in the initial MVP.                                                                                                                                                                                                                                                    | Confirmed | Avoids building a "shadow workflow" in email before the in-app experience is proven; keeps sensitive content out of inboxes until the model is settled.                                                                                                                                                                                                                                                                                                                                      | Pilot feedback indicates email is required earlier.                                                                                 |
| D-012 | 2026-08-12 | Attachments are intended for a TEACH-owned, restricted Google Shared Drive — never an individual employee's personal Drive.                                                                                                                                                                                                                                    | Confirmed | Keeps attachment ownership and access control with the organization rather than an individual account.                                                                                                                                                                                                                                                                                                                                                                                       | TEACH changes its file-storage platform.                                                                                            |
| D-013 | 2026-08-12 | Configuration access and confidential-content access are separate permissions.                                                                                                                                                                                                                                                                                 | Confirmed | A system administrator configuring the platform should not automatically be able to read confidential ticket content.                                                                                                                                                                                                                                                                                                                                                                        | Not anticipated; revisit only if this proves operationally unworkable.                                                              |
| D-014 | 2026-08-12 | The project will be implemented in small, approved phases.                                                                                                                                                                                                                                                                                                     | Confirmed | Reduces risk of overbuilding before the core workflow is validated with real users.                                                                                                                                                                                                                                                                                                                                                                                                          | Not anticipated; revisit only by explicit leadership direction.                                                                     |
| D-015 | 2026-08-12 | Use the stable Next.js App Router with strict TypeScript and a `src/` directory structure.                                                                                                                                                                                                                                                                     | Confirmed | Matches the documented future technical direction; App Router is the current stable Next.js paradigm and strict TypeScript catches type errors before runtime.                                                                                                                                                                                                                                                                                                                               | Next.js deprecates the App Router or a materially different framework is selected.                                                  |
| D-016 | 2026-08-12 | Use npm with a single committed `package-lock.json` as the sole package manager.                                                                                                                                                                                                                                                                               | Confirmed | Avoids multiple lockfiles and keeps installs reproducible across contributors and CI.                                                                                                                                                                                                                                                                                                                                                                                                        | Team standardizes on a different package manager.                                                                                   |
| D-017 | 2026-08-12 | Adopt Node.js 24 LTS as the runtime baseline (`.nvmrc` and `package.json` `engines`).                                                                                                                                                                                                                                                                          | Confirmed | Node 24 is the current LTS release at the start of implementation, giving the longest support window.                                                                                                                                                                                                                                                                                                                                                                                        | Node 24 reaches end-of-life or a newer LTS becomes the team standard.                                                               |
| D-018 | 2026-08-12 | Use Tailwind CSS for the minimal application shell's styling.                                                                                                                                                                                                                                                                                                  | Confirmed | Matches the documented future technical direction and avoids hand-rolled CSS for a simple, responsive shell.                                                                                                                                                                                                                                                                                                                                                                                 | A design system or component library is adopted later.                                                                              |
| D-019 | 2026-08-12 | Use Vitest and React Testing Library for component-level tests.                                                                                                                                                                                                                                                                                                | Confirmed | Vitest integrates cleanly with the Vite-based tooling Next.js's ecosystem already uses, and React Testing Library encourages accessible, user-facing queries over implementation detail.                                                                                                                                                                                                                                                                                                     | A different testing framework becomes the team standard, or end-to-end testing is introduced in a later phase.                      |
| D-020 | 2026-08-12 | Use GitHub Actions for automated quality checks (lint, format, typecheck, tests, build) on every push and pull request.                                                                                                                                                                                                                                        | Confirmed | Keeps quality checks automatic and visible without requiring a separate CI provider; uses only read-only permissions and no secrets.                                                                                                                                                                                                                                                                                                                                                         | The team adopts a different CI provider or adds deployment automation requiring different permissions.                              |
| D-021 | 2026-08-12 | Use Drizzle ORM and Drizzle Kit for typed schema definition and versioned SQL migration generation.                                                                                                                                                                                                                                                            | Confirmed | Drizzle keeps migrations as plain, reviewable SQL rather than an opaque engine format, and its TypeScript-first schema matches the project's typed stack without adding a heavier ORM.                                                                                                                                                                                                                                                                                                       | The team adopts a different ORM/migration toolchain.                                                                                |
| D-022 | 2026-08-12 | Keep the database layer provider-neutral (plain PostgreSQL dialect, no provider-specific SDK) until a managed provider is selected.                                                                                                                                                                                                                            | Confirmed | No managed PostgreSQL provider has been chosen yet; avoiding provider-specific libraries keeps the schema and connection code portable across whichever provider is eventually selected.                                                                                                                                                                                                                                                                                                     | A managed PostgreSQL provider is selected.                                                                                          |
| D-023 | 2026-08-12 | Use node-postgres (`pg`) with a lazy, server-side-only connection-pool factory for database access.                                                                                                                                                                                                                                                            | Confirmed | `pg` is the standard, provider-neutral PostgreSQL driver for Node; a lazy factory ensures importing database modules, running `next build`, or loading the status page never requires a database connection or environment variable.                                                                                                                                                                                                                                                         | A different runtime driver becomes necessary for a selected provider.                                                               |
| D-024 | 2026-08-12 | Commit versioned SQL migrations (and Drizzle's migration journal/snapshots) generated from the typed schema, reviewed before commit.                                                                                                                                                                                                                           | Confirmed | Plain committed SQL is auditable in code review and reproducible across environments; migrations are generated from the schema, never hand-written against it, to keep them from silently disagreeing with the types.                                                                                                                                                                                                                                                                        | Not anticipated; revisit only if the migration tool changes.                                                                        |
| D-025 | 2026-08-12 | Migrations and seeds run only via explicit administrative commands (`db:migrate`, `db:seed`), never automatically during install, build, start, or deployment.                                                                                                                                                                                                 | Confirmed | Automatic migrations during a build or deploy risk silently altering a shared database; requiring an explicit, human-triggered command keeps schema changes deliberate and reviewable.                                                                                                                                                                                                                                                                                                       | Not anticipated; revisit only by explicit leadership direction.                                                                     |
| D-026 | 2026-08-12 | Canonical reference records (the TEACH organization, schools, and service locations) use stable, hand-assigned UUIDs that never regenerate between seed runs.                                                                                                                                                                                                  | Confirmed | Stable IDs make the seed idempotent and let other data reference these records predictably across environments and reseeds.                                                                                                                                                                                                                                                                                                                                                                  | Not anticipated; revisit only if reference data is restructured.                                                                    |
| D-027 | 2026-08-12 | Use PGlite strictly as in-memory, test-only database infrastructure for the automated integration test suite, never as a production option.                                                                                                                                                                                                                    | Confirmed | PGlite lets the test suite prove real PostgreSQL migrations and constraints work end-to-end with no external database or credentials, while keeping the production database question fully open.                                                                                                                                                                                                                                                                                             | Not anticipated; revisit only if the testing strategy changes.                                                                      |
| D-028 | 2026-08-12 | The managed production PostgreSQL hosting provider remains an open decision.                                                                                                                                                                                                                                                                                   | Confirmed | No provider has been evaluated or selected; the schema and connection pattern are deliberately provider-neutral until this is resolved.                                                                                                                                                                                                                                                                                                                                                      | A managed PostgreSQL provider is evaluated and selected.                                                                            |
| D-029 | 2026-08-13 | Use Better Auth (`better-auth` + `@better-auth/drizzle-adapter`), pinned to the exact stable `1.6.27` release, for all OAuth/OIDC, session, and CSRF handling.                                                                                                                                                                                                 | Confirmed | A maintained authentication library avoids hand-rolled OAuth/JWT/session/CSRF code, which is high-risk to get right; pinning to an exact stable release (never a beta/RC) keeps behavior predictable and reviewable.                                                                                                                                                                                                                                                                         | A security or maintenance issue requires a different version or library.                                                            |
| D-030 | 2026-08-13 | Use Google's immutable per-account `sub` claim, never the email address, as the identity anchor for a provider account record.                                                                                                                                                                                                                                 | Confirmed | An email address can change while `sub` cannot; anchoring identity to a mutable value risks silently attaching one person's history to another.                                                                                                                                                                                                                                                                                                                                              | Not anticipated; revisit only if Google changes its identity-claim guarantees.                                                      |
| D-031 | 2026-08-13 | Every account provisioned through Google Workspace sign-in is fixed to the canonical TEACH organization and the nonprivileged Requester base role, enforced redundantly at the API-field, hook, and database-constraint layers.                                                                                                                                | Confirmed | Phase 3 is scoped to authentication only; every account must start from the same, deliberately minimal, non-elevated state, enforced at multiple layers so no single code change can silently grant more.                                                                                                                                                                                                                                                                                    | Phase 4 introduces a role/permission system that supersedes the fixed Requester default.                                            |
| D-032 | 2026-08-13 | Request only the minimal OpenID Connect scopes (`openid`, `email`, `profile`) from Google, and never persist an OAuth access token, refresh token, ID token, or password.                                                                                                                                                                                      | Confirmed | The only use of Google here is identity verification at sign-in; no broader scope or stored credential is needed, and one not requested or stored can never leak.                                                                                                                                                                                                                                                                                                                            | A future phase needs to act on a user's behalf against a Google API, requiring separately reviewed scope/storage additions.         |
| D-033 | 2026-08-13 | Use database-backed sessions with an 8-hour maximum lifetime and Secure, `HttpOnly`, `SameSite=Lax` cookies.                                                                                                                                                                                                                                                   | Confirmed | Database-backed sessions can be revoked server-side by deleting a row, unlike stateless JWTs; an 8-hour cap limits the exposure window of a stolen session without disrupting a normal workday.                                                                                                                                                                                                                                                                                              | Operational experience shows the session lifetime is too short or too long for real usage patterns.                                 |
| D-034 | 2026-08-13 | A failed sign-in attempt always shows the same generic denial message, regardless of which specific verification check failed.                                                                                                                                                                                                                                 | Confirmed | Revealing which check failed would let an outsider probe for which email addresses or organizations exist; a single generic message leaks nothing.                                                                                                                                                                                                                                                                                                                                           | Not anticipated; revisit only if support workflows require more specific in-app guidance.                                           |
| D-035 | 2026-08-13 | Build only a simplified three-level MVP access-control model for Phase 4 — Requester, Department Agent, System Administrator — before any richer role/permission/campus-grant system.                                                                                                                                                                          | Confirmed | The core help-desk workflow (tickets, routing, communication) doesn't exist yet; building department-manager roles, campus grants, confidential queues, and audit workflows before that core exists risks over-engineering a permission system for a workflow that hasn't been validated.                                                                                                                                                                                                    | The core ticket workflow (Phase 5+) is working and validated, and richer authorization is explicitly approved.                      |
| D-036 | 2026-08-13 | Model "department agent" as a simple membership table — presence of a row means the user is an agent for that department — instead of a role-grant framework.                                                                                                                                                                                                  | Confirmed | A plain membership table is the smallest structure that satisfies "a user may belong to IT, Facilities, or both" without introducing manager levels, expiring grants, or a capability matrix the MVP doesn't need yet.                                                                                                                                                                                                                                                                       | Department-manager-level distinctions become a confirmed, approved requirement.                                                     |
| D-037 | 2026-08-13 | Designate a system administrator with one explicit, database-set boolean column (`user.is_system_administrator`), defaulting `false`, never seeded, never client-settable.                                                                                                                                                                                     | Confirmed | This is the smallest design that fits the existing `user` table cleanly; a hidden administrator, bootstrap account, or email-based allowlist would be a harder-to-audit shortcut around the same requirement.                                                                                                                                                                                                                                                                                | Multiple administrator levels (e.g., configuration vs. confidential-content authority) become a confirmed requirement.              |
| D-038 | 2026-08-13 | Keep the authorization module (`src/authz/`) independent of Better Auth's initialization; resolve the current actor from a validated session's user id plus a fresh, parameter-free database read, never from client-supplied role/membership claims.                                                                                                          | Confirmed | Testability without Google credentials and a hard architectural guarantee against trusting forged client input are both satisfied by the same design: the actor-resolution function's signature has no field a client could use to claim a role.                                                                                                                                                                                                                                             | Not anticipated; revisit only if the authorization module's responsibilities expand significantly.                                  |
| D-039 | 2026-08-13 | Treat service locations as ticket routing/filtering metadata during the MVP, not a permission-scoping system.                                                                                                                                                                                                                                                  | Confirmed | Campus-scoped visibility (e.g., for principals) is a deferred, post-MVP concern (Phase 9); introducing location-based access control before it's needed would add complexity the current phase doesn't require.                                                                                                                                                                                                                                                                              | Campus/principal visibility (Phase 9) becomes a confirmed, approved requirement.                                                    |
| D-040 | 2026-08-13 | Generate ticket numbers with a database identity/sequence column (`tickets.ticket_number GENERATED ALWAYS AS IDENTITY`), never from a record count, timestamp, random value, or client-side logic.                                                                                                                                                             | Confirmed | Only a database sequence guarantees uniqueness under concurrent ticket creation; record counts race under concurrency, timestamps can collide, and any client-side generation could be forged or duplicated.                                                                                                                                                                                                                                                                                 | Not anticipated; revisit only if ticket numbers must be portable across a future database migration/merge.                          |
| D-041 | 2026-08-13 | Implement a small, hardcoded Phase 5 status subset (`submitted`, `in_progress`, `waiting_for_requester`, `resolved`, `closed`, `reopened`) instead of the full documented 14-state lifecycle, preserving the Resolved/Closed distinction exactly.                                                                                                              | Confirmed | The full lifecycle's Triaged/Assigned granularity, Waiting for Vendor, Duplicate, Referred to Another Department, Converted to Project, and Emergency Escalation states all depend on features (queue ownership, vendor tracking, SLA model) that don't exist yet — building the states without the features they support would be premature.                                                                                                                                                | The features underlying a deferred state (e.g., vendor tracking, queue-owner distinction) become a confirmed requirement.           |
| D-042 | 2026-08-13 | Phase 5 ticket comments are ordinary, append-only, shared (requester + support staff) conversation only — no internal/private notes, attachments, editing, deletion, reactions, rich text, or email ingestion.                                                                                                                                                 | Confirmed | Internal notes require a distinct visibility rule from ordinary comments (per `PROJECT_FOUNDATION.md` Section 8) that hasn't been built or tested yet; introducing them before that visibility boundary exists risks an internal note leaking to a requester.                                                                                                                                                                                                                                | Phase 8 (Requester Communication and Internal Notes) is explicitly scoped and implemented.                                          |
| D-043 | 2026-08-13 | Enforce "a ticket's category belongs to its department" and "a ticket's department/location/requester/assignee belongs to its organization" with composite database foreign keys, extending the same pattern already used for `department_memberships` in Phase 4.                                                                                             | Confirmed | Database-level enforcement holds even against a direct, non-service-layer write, consistent with this project's defense-in-depth approach established in Phases 2–4; reusing an established pattern keeps the schema style consistent rather than introducing a new enforcement mechanism.                                                                                                                                                                                                   | Not anticipated; revisit only if the composite-FK pattern proves too costly at production scale.                                    |
| D-044 | 2026-08-13 | Extend the existing Phase 4 authorization module with one additional `manage_ticket` action (department agent or system administrator only) rather than building a second, parallel authorization system for tickets.                                                                                                                                          | Confirmed | A requester must be able to view and comment on their own ticket but never change its status, priority, or assignment — a distinction the existing `access_ticket` action doesn't make. Adding one action to the existing centralized module is simpler than a separate ticket-specific authorization path.                                                                                                                                                                                  | Not anticipated; revisit only if ticket authorization needs diverge significantly from other resource types.                        |
| D-045 | 2026-08-13 | Requester-facing routes address a ticket by its human-friendly ticket number (e.g. `/requests/TKT-000001`), parsed with a strict pattern; the internal database id is never exposed in a URL or rendered as visible text.                                                                                                                                      | Confirmed | The ticket number already exists as the intended human-facing identifier (D-040); reusing it in the URL avoids exposing an internal UUID that carries no meaning to staff and would otherwise need separate masking.                                                                                                                                                                                                                                                                         | Not anticipated; revisit only if ticket numbers must ever be treated as sensitive.                                                  |
| D-046 | 2026-08-13 | A ticket that does not exist and a ticket that exists but the current actor is not authorized to view return an identical `null` from every Phase 6 query function, rendered as the same generic "not found" page.                                                                                                                                             | Confirmed | Returning a different result for "doesn't exist" versus "exists but you can't see it" would let a requester probe for other people's ticket numbers; a single indistinguishable outcome leaks nothing, consistent with the sign-in denial precedent (D-034).                                                                                                                                                                                                                                 | Not anticipated; revisit only if a future role needs a distinguishable "access denied" message.                                     |
| D-047 | 2026-08-13 | Each Phase 6 page independently calls a shared `requireActiveActor(callbackPath)` helper with its own accurate return path, rather than enforcing authentication once in a shared `/requests` layout.                                                                                                                                                          | Confirmed | Every page already needs the resolved actor for its own data fetching, and a Server Component layout has no mechanism to pass a resolved value down to a nested page; a single per-page check avoids a redundant second lookup and keeps the sign-in callback URL accurate to where the user was headed.                                                                                                                                                                                     | Not anticipated; revisit only if Next.js adds a supported way to share resolved data from layout to page.                           |
| D-048 | 2026-08-13 | Friendly status and priority labels (e.g. `waiting_for_requester` → "Waiting for you") live in one presentation-only mapping (`src/tickets/labels.ts`); no internal database enum value is ever rendered in requester-facing UI text.                                                                                                                          | Confirmed | Staff should never need to interpret an internal code name; centralizing the mapping in one file keeps every requester-facing surface consistent and makes a future wording change a one-file edit.                                                                                                                                                                                                                                                                                          | The documented status or priority set changes (Phase 7+).                                                                           |
| D-049 | 2026-08-13 | A ticket comment's requester-vs-support-team origin is computed server-side (`authorId === ticket.requesterId`) and shown with a visible text label, never by color alone.                                                                                                                                                                                     | Confirmed | Color-only distinctions are inaccessible to colorblind users and to plain-text/high-contrast rendering; a text label carries the same information for everyone.                                                                                                                                                                                                                                                                                                                              | Phase 8 introduces roles beyond a single requester/support-team split (e.g. multiple support participants needing distinct labels). |
| D-050 | 2026-08-13 | Ticket creation and comment submission use Next.js Server Actions, not a REST API route, consistent with the existing `/sign-in` and `/account` pattern.                                                                                                                                                                                                       | Confirmed | A Server Action already runs server-side with direct access to the resolved actor and the Phase 5 ticket service, avoiding a redundant API route, request parsing, and client-side fetch/error-handling layer for a same-origin form submission.                                                                                                                                                                                                                                             | A future phase needs the same mutation reachable from a non-browser client.                                                         |
| D-051 | 2026-08-14 | Add one actor-shape predicate, `isSupportStaff(actor)`, to the existing `src/authz/policy.ts` (true for a system administrator or any department membership) rather than a new authorization module, to gate `/support` and the shared navigation's Support Queue link.                                                                                        | Confirmed | This is a check about the actor's own shape, not a resource decision, so it doesn't fit `authorize()`'s `(actor, action)` signature; adding one small function to the existing centralized module keeps authorization logic in one place instead of starting a second one.                                                                                                                                                                                                                   | Not anticipated; revisit only if support-staff eligibility grows more complex than "administrator or any membership."               |
| D-052 | 2026-08-14 | The support ticket workspace (`/support/[ticketNumber]`) is gated by the existing `manage_ticket` authorization action, not `access_ticket` — an ordinary requester never reaches it, even for a ticket they own.                                                                                                                                              | Confirmed | `access_ticket` deliberately also grants a requester their own ticket, which is correct for `/requests/[ticketNumber]` but wrong for the support workspace; `manage_ticket` already encodes exactly "department agent or system administrator," the same rule the support workspace needs, so no new authorization action was created.                                                                                                                                                       | Not anticipated; revisit only if a requester ever needs partial support-workspace visibility.                                       |
| D-053 | 2026-08-14 | A closed ticket's finality is enforced inside the ticket service itself (`assertNotClosed` in `addTicketComment`, `updateTicketPriority`, and `assignTicket`), not only by hiding controls in the UI.                                                                                                                                                          | Confirmed | Phase 5 already made status transitions out of "closed" impossible via `canTransitionTicketStatus`, but comments, priority, and assignment had no equivalent guard; a UI-only restriction would not hold against a direct service call, so the same rule now holds at the layer everything else is enforced at.                                                                                                                                                                              | Not anticipated; revisit only if a documented exception to "closed is final" is approved.                                           |
| D-054 | 2026-08-14 | The "Assign to me" action never reads a client-supplied user id at all — it always assigns the actor resolved from the validated server session, in a dedicated server action separate from the general assignee-selector action.                                                                                                                              | Confirmed | A one-click "assign to me" control has no legitimate reason to accept any external identity value; giving it its own action with no form-data-derived id removes an entire class of forged-assignee concern for that path, while the general selector still re-validates through the same `assignTicket` service rule.                                                                                                                                                                       | Not anticipated; revisit only if "assign to me" needs to support assigning on another agent's behalf.                               |
| D-055 | 2026-08-14 | Every support-queue filter (department, location, status, assignment) is validated server-side in `src/tickets/support-queries.ts` and silently falls back to its safe default when invalid or unauthorized, rather than raising an error.                                                                                                                     | Confirmed | A tampered or stale query-string value (an old department id, a garbage status) should never break the page or let someone infer whether a value would otherwise have been valid; falling back to the same default the page would show anyway is the safest, simplest response.                                                                                                                                                                                                              | Not anticipated; revisit only if a future filter needs to distinguish "ignored" from "not set" in the UI.                           |
| D-056 | 2026-08-14 | The support workspace's status and priority controls only ever offer values the service would actually accept — valid next statuses computed from the existing `canTransitionTicketStatus` rule, and every priority except the ticket's current one — rather than offering the full set and relying on a rejected submission.                                  | Confirmed | Narrowing the choices to only legal values is simpler and friendlier than presenting an option the server will reject; the service layer still re-validates independently, so this is a UX improvement layered on an unchanged authorization/validation boundary, not a relaxation of it.                                                                                                                                                                                                    | Not anticipated; revisit only if the transition rule itself becomes configurable.                                                   |
| D-057 | 2026-08-14 | The authenticated navigation (`src/app/app-nav.tsx`) is one shared component used by both the requester layout and the new support layout, rather than a separate nav per section, and decides whether to show Support Queue by resolving the current actor itself.                                                                                            | Confirmed | Duplicating the same brand/links/sign-out markup per section would drift over time; a single component that resolves its own actor keeps each layout simple and presentational, at the cost of one extra (cheap, indexed) actor lookup per page render — an accepted tradeoff already established by the Phase 6 per-page `requireActiveActor` pattern (D-047).                                                                                                                              | Not anticipated; revisit only if actor resolution becomes expensive enough to need request-level caching.                           |
| D-058 | 2026-08-14 | `/admin` and every `src/admin/` mutation are gated by the existing `administer` action already defined in `src/authz/policy.ts` (Phase 4) — no new "administrator" role check or authorization module was introduced.                                                                                                                                          | Confirmed | The Phase 4 policy module already draws exactly the distinction Phase 8 needs ("is this actor a system administrator"); adding a second, parallel check would only risk the two silently drifting apart over time.                                                                                                                                                                                                                                                                           | Not anticipated; revisit only if administration needs a decision `authorize()`'s actor-only shape cannot express.                   |
| D-059 | 2026-08-14 | The People and Access page's IT/Facilities toggles directly add or remove a `department_memberships` row — department membership already _is_ department-agent access (Phase 4, D-036); no separate "agent role" concept was introduced.                                                                                                                       | Confirmed | Reusing the existing membership table keeps exactly one source of truth for "is this person a department agent" across the requester, support, and now administration surfaces.                                                                                                                                                                                                                                                                                                              | Department-manager-level distinctions (already deferred in D-036) become a confirmed requirement.                                   |
| D-060 | 2026-08-14 | Adding a department membership uses `onConflictDoNothing()` against the existing `department_memberships_user_department_unique` constraint, rather than a check-then-insert.                                                                                                                                                                                  | Confirmed | A database-enforced idempotent insert cannot race with itself the way an application-level "check, then insert" can; it also needs no new constraint, since the unique index already existed for Phase 4's own duplicate-membership rule.                                                                                                                                                                                                                                                    | Not anticipated; revisit only if department memberships ever need per-row metadata that makes "add" non-idempotent.                 |
| D-061 | 2026-08-14 | An administrator can never deactivate their own account or remove their own administrator access, enforced in `src/admin/admin-service.ts` itself — not only by hiding the button in the UI.                                                                                                                                                                   | Confirmed | Without this, a single click (or a direct call bypassing the UI) could leave an organization with zero active administrators and no way to recover access short of a manual database operation; the safeguard belongs at the same layer as every other authorization decision, not merely in what a page happens to render.                                                                                                                                                                  | Not anticipated; revisit only if a documented multi-administrator recovery process is approved.                                     |
| D-062 | 2026-08-14 | The People and Access page displays each staff member's email address, unlike every other page in the application.                                                                                                                                                                                                                                             | Confirmed | An administrator managing access needs to positively identify the correct person among possibly similar display names; this page is restricted to system administrators only, and still never shows a provider account id, Google subject, session data, token, or other authentication metadata.                                                                                                                                                                                            | Not anticipated; revisit only if display names are confirmed to be unique enough to drop email entirely.                            |
| D-063 | 2026-08-14 | Requester status is displayed on the People and Access page but is never a toggleable action.                                                                                                                                                                                                                                                                  | Confirmed | `user.base_role` is fixed to `'requester'` for every account at the database level (D-031) — there is no revocable "requester" flag to grant or remove, so no such control was built.                                                                                                                                                                                                                                                                                                        | The fixed base-role invariant (D-031) is revisited.                                                                                 |
| D-064 | 2026-08-14 | A native `window.confirm()` prompt is required only before deactivating a user or granting/removing system-administrator access — not before adding/removing department access or activating a user.                                                                                                                                                           | Confirmed | Deactivation and administrator-access changes are the two categories of mistake that are hardest to notice and most consequential to undo quickly; matching confirmation friction to actual severity, rather than requiring it everywhere, keeps the page usable.                                                                                                                                                                                                                            | Not anticipated; revisit only if a lower-severity action proves to cause frequent mistakes in practice.                             |
| D-065 | 2026-08-14 | Phase 8 builds the People and Access management UI but seeds no administrator; the first real system administrator's designation remains a separately approved, manual operational step, exactly as already documented for Phase 3/4.                                                                                                                          | Confirmed | Building the _tool_ to manage access is safe and testable with only synthetic data; actually designating a real person as administrator is an operational, security-sensitive decision this repository's automated work has never been authorized to make on its own.                                                                                                                                                                                                                        | The separately approved first-administrator bootstrap is carried out and documented.                                                |
| D-066 | 2026-08-14 | Every friendly error/empty/access-denied state (the new root `error.tsx`, `global-error.tsx`, `not-found.tsx`, and the refactored access-denied blocks) shares one small `FriendlyState` component instead of a design-system-scale component library.                                                                                                         | Confirmed | The existing access-denied/not-found blocks were already near-identical JSX; extracting the smallest component that covers both Server- and Client-Component call sites (no hooks, `href`-or-`onClick` actions) removes the duplication without introducing a new abstraction layer the MVP doesn't need.                                                                                                                                                                                    | Not anticipated; revisit only if a future phase needs a materially different friendly-state shape (e.g. an icon or illustration).   |
| D-067 | 2026-08-14 | The root error boundary (`error.tsx`) shows one generic message ("We couldn't load this page...") for every unexpected error, including a database-connection failure, rather than sniffing the error to show a database-specific message.                                                                                                                     | Confirmed | Attempting to detect "this was specifically a Postgres connection error" inside a client error boundary risks either missing cases or leaking implementation detail (a driver name, a connection-string fragment) into the message; one safe, generic message covers every case correctly.                                                                                                                                                                                                   | Not anticipated; revisit only if a specific failure mode needs a materially different next action, not just different wording.      |
| D-068 | 2026-08-14 | `npm run readiness:check` is a new, standalone command and is deliberately **not** added to `npm run check`.                                                                                                                                                                                                                                                   | Confirmed | CI (`.github/workflows/quality.yml`) runs `npm run check` with no secrets configured, by design; folding a command whose entire purpose is validating real secrets into the secretless CI path would either make CI depend on secrets it intentionally doesn't have, or make the check meaningless in CI.                                                                                                                                                                                    | Not anticipated; revisit only if CI is given a dedicated, non-production secret set to validate against.                            |
| D-069 | 2026-08-14 | The first-administrator bootstrap logic (`bootstrapFirstSystemAdministrator` in `src/admin/bootstrap.ts`) is a separate function from `src/admin/admin-service.ts`'s `setSystemAdministrator`, and does not call `authorize()`/`assertAdministrator()` at all.                                                                                                 | Confirmed | At the moment this command needs to run, no administrator exists yet to authorize the action — the existing administration module's every function assumes an already-authorized acting administrator, which cannot be true here by definition; a separate, operator-level (not client-level) path is the only way to bootstrap the very first one.                                                                                                                                          | Not anticipated; revisit only if a different bootstrap trust model is approved.                                                     |
| D-070 | 2026-08-14 | `DATABASE_MIGRATION_URL` is documented as remaining operator-only (used locally or in a controlled deployment step for `db:migrate`, `db:seed`, and `admin:bootstrap`) and is explicitly **not** added to Vercel's environment variables in [`DEPLOYMENT.md`](DEPLOYMENT.md).                                                                                  | Confirmed | The running application never needs an administrative database connection at runtime — only `DATABASE_URL` is required; giving the deployed application itself access to the same elevated connection used for migrations and administrator bootstrap would widen its blast radius for no operational benefit.                                                                                                                                                                               | A later, separately approved deployment design specifically requires the running application to hold this connection.               |
| D-071 | 2026-08-14 | `user`'s email `CHECK` constraint (`user_verified_teachps_email_check`) is relaxed from `@teachps.org`-exact to a domain-agnostic, verified/lowercased/well-formed check (`user_verified_email_check`, migration `0005`); domain eligibility is enforced only at the application layer (`src/auth/google-identity-policy.ts`), selected by `AUTH_ACCESS_MODE`. | Confirmed | A single static database constraint cannot vary by a runtime environment variable, and invite-only mode must accept a personal Gmail address or any other Workspace domain; moving domain enforcement entirely to the application layer (already covered by `evaluateGoogleWorkspaceIdentity`/`evaluateGoogleInviteOnlyIdentity` and their tests) lets the same schema serve both access modes without weakening verification/normalization, which remain database-enforced unconditionally. | Not anticipated; revisit only if a database-level domain restriction is required again for a single-mode deployment.                |
| D-072 | 2026-08-14 | The new `auth_invitations` table (migration `0005`) is deliberately minimal: one status enum (`pending`/`accepted`/`revoked`) with a `CHECK`-enforced status/timestamp shape, a partial unique index limiting only _pending_ rows per email (not a plain unique constraint), and no seed data — not a generalized invitation platform.                         | Confirmed | The task is a small controlled pilot, not a full invitation system; a partial (not plain) unique index is required specifically so that re-inviting a previously revoked address creates a fresh row without deleting history, which the requirements explicitly call for.                                                                                                                                                                                                                   | A future phase needs invitation expiry, bulk invitations, or multi-organization support.                                            |
| D-073 | 2026-08-14 | Invite-only provisioning denies a noninvited sign-in by returning `false` from the existing `databaseHooks.user.create.before` hook (aborting Better Auth's user-creation transaction) rather than adding a separate pre-flight API call or a custom OAuth callback route.                                                                                     | Confirmed | Better Auth's own `createWithHooks` contract already treats a `before` hook returning `false` as "abort creation," and the resulting error is already routed through the existing, tested `/sign-in?error=1` friendly-denial path — reusing this mechanism needed no new API surface and no change to how failures are already handled.                                                                                                                                                      | Not anticipated; revisit only if a future Better Auth version changes this hook contract.                                           |
| D-074 | 2026-08-14 | `bootstrapFirstSystemAdministrator` takes an explicit `accessMode` parameter and applies mode-specific eligibility (exact `AUTH_ALLOWED_DOMAIN` match in workspace mode; a linked Google account and an accepted invitation, no domain restriction, in invite-only mode) rather than one fixed `@teachps.org` rule.                                            | Confirmed | The guarded bootstrap command must work correctly under either deployment mode without ever creating a user or changing an organization in either case; parameterizing eligibility by the same `AuthAccessMode` type already used everywhere else keeps exactly one source of truth for "which mode is active" instead of a second, parallel notion of mode.                                                                                                                                 | Not anticipated; revisit only if a third access mode is introduced.                                                                 |
| D-075 | 2026-08-14 | The guarded `npm run access:invite` CLI (`src/auth/invite-bootstrap.ts`) mirrors `admin:bootstrap`'s exact safeguard shape (dry-run default, `--apply` + matching `--confirm-email`, `DATABASE_MIGRATION_URL`, no actor/`authorize()` call) rather than reusing the admin-authorized `src/admin/invitations.ts` service.                                       | Confirmed | The very first invitation, like the very first administrator, must be creatable before any administrator exists to authorize the action through `/admin` — an operator-level, guarded CLI is the only way to bootstrap either one, and reusing the already-proven `admin:bootstrap` safeguard shape keeps the two guarded commands consistent for whoever runs them.                                                                                                                         | Not anticipated; revisit only if a different bootstrap trust model is approved.                                                     |
| D-076 | 2026-08-14 | The `/admin` Pilot Invitations section (admin-authorized, via `src/admin/invitations.ts`) rejects a duplicate pending invitation outright, while the guarded CLI treats the same situation as an idempotent no-op.                                                                                                                                             | Confirmed | The admin UI is an interactive form that can show immediate, specific feedback ("already pending") the moment an administrator tries to duplicate an invitation, whereas the CLI is meant to be safely re-run (e.g. in a script or retried by hand) without erroring on a state that requires no actual change — the two audiences warrant different responses to the identical underlying state.                                                                                            | Not anticipated; revisit only if the CLI gains its own interactive confirmation UI.                                                 |

## Open Decisions

| Item                                                                                                                                | Notes                                                                                                                                                           |
| ----------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Final production database and hosting provider                                                                                      | PostgreSQL and Vercel-compatible deployment are the intended direction; the specific managed provider(s) have not been selected.                                |
| Google OAuth client and deployment ownership                                                                                        | Who owns/administers the Google Cloud OAuth client and production deployment account has not been determined.                                                   |
| Exact school holiday calendar administration process                                                                                | Holidays/closures must exclude from service clocks, but who maintains the calendar and how it is entered/updated is undecided.                                  |
| Attachment retention and deletion schedule                                                                                          | No retention period has been set for uploaded attachments once the attachment feature is built.                                                                 |
| Maximum attachment file size and permitted file types                                                                               | No size limit or file-type allowlist has been confirmed.                                                                                                        |
| Resolution confirmation period before automatic closure                                                                             | The window during which a requester may confirm or reopen before a ticket auto-closes has not been finalized.                                                   |
| Final notification sender address                                                                                                   | The "from" address for outbound email notifications (once built) has not been decided.                                                                          |
| Production incident and backup procedures                                                                                           | No incident-response runbook or backup/restore procedure exists yet.                                                                                            |
| **⚠️ Security & Governance Review — Whether the repository should remain public before application code or configuration is added** | This item requires an explicit security and governance decision before Phase 1 begins. **No repository visibility change has been made as part of this phase.** |
